# Host-seitige Python-Abhängigkeiten (ausserhalb von Docker).
#
# Der Classifier-Service selbst läuft im Container und installiert seine
# Abhängigkeiten aus classifier-service/requirements.txt. Diese Datei deckt
# nur ab, was man auf dem Host braucht, um den Load-Generator auszuführen.

# --- Load-Generator (scripts/loadgen.py) ---
httpx==0.27.2
